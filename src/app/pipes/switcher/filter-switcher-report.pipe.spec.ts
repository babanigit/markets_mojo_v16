import { FilterSwitcherReportPipe } from './filter-switcher-report.pipe';

describe('FilterSwitcherReportPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSwitcherReportPipe();
    expect(pipe).toBeTruthy();
  });
});
