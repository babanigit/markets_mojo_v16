import { FilterChoiceResultPipe } from './filter-choice-result.pipe';

describe('FilterChoiceResultPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChoiceResultPipe();
    expect(pipe).toBeTruthy();
  });
});
