import { HighestObjPipe } from './highest-obj.pipe';

describe('HighestObjPipe', () => {
  it('create an instance', () => {
    const pipe = new HighestObjPipe();
    expect(pipe).toBeTruthy();
  });
});
