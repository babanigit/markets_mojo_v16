import { TwoCommasPipe } from './two-commas.pipe';

describe('TwoCommasPipe', () => {
  it('create an instance', () => {
    const pipe = new TwoCommasPipe();
    expect(pipe).toBeTruthy();
  });
});
