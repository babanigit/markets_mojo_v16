import { SnapshotFilterPipe } from './snapshot-filter.pipe';

describe('SnapshotFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SnapshotFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
