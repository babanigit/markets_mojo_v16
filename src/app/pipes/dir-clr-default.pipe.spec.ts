import { DirClrDefaultPipe } from './dir-clr-default.pipe';

describe('DirClrDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new DirClrDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
